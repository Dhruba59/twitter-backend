import express from "express";

const router = express.Router();

// router.get("/userwise/all", getUserwiseAllTweets);

// router.get("/:id", getTweetById);

// router.post("/", createTweet);

// router.put("/:id/addTags", addTags);

// router.get("/following_user_tweets", getAllTweetsOfFollowingUser);

export default router;
